let frase = "Hola soy Natalia"; // String o cadena de texto
let anio = 2026;                // Numero entero / int
let interes = 2.7;              // Numero decimal / float / double
let mayorEdad = true;           // Boleano
let vacia;                      // undefined
let nula = null;                // null

let frutas = ["fresa", "sandia", "naranja"];    // array
let heroe = {nombre: "Batman", universo: "DC"}; // Objeto

console.log(frase, interes, mayorEdad);


// Operador de tipo (typeof)
console.log(typeof frase);          // Te dice el tipo de dato que es

console.log(Array.isArray(frutas)); // Comprueba si es un array