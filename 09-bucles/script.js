// Bucle for
// Bucle es una estructura de control que ejecuta un bloque de codigo
// varias veces de forma automatiica meintras se cumpla una condición

/*
for (inicilización; condición; incremento){

    // Codigo que se ejcuta repetidamente

}
*/

// Ejemplo:
let limite = 177;

for(contador = 0; contador <= limite; contador++){

    // bloque de codigo que se ejecutará
    console.log("Vamos por el numero: "+contador);
    
}